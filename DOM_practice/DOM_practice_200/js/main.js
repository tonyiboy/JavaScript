// const elements = Array.from(document.getElementsByClassName("para"))

// elements.forEach(element => {
//     console.log(element);
// });

const elements = document.getElementsByClassName("para")

for(let i = 0; i < elements.length; i++) {
    console.log(elements[i]);
}






